<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackground</item>
       <!-- <item name="android:windowBackground">@color/trimble_background_gray</item>-->
        <item name="radioButtonStyle">@style/TrimbleRadioButton</item>

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/trimble_header_blue</item>
        <item name="colorPrimaryDark">@android:color/background_dark</item>

        <!-- ensure that alert dialogs are consistently shown on pre-lollipop and lollipop devices -->
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
    </style>
</resources>